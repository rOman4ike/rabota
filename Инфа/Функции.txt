Function Expression (AFE Anonymous function expression)
	let sayHi = function() {
		alert( "Привет" );
	};
	
	Вызываются, чтобы не загрязнять глобальную область видимости.

	Function expression можно передать функцию в качестве аргумента (Declaration нельзя)

Function Declaration
	function sayHi() {
		alert( "Привет" );
	}
	
	Объявления функций загружаются в контекст выполнения до выполнения кода. Это известно как подъем (hoisting), то есть вы можете использовать функцию до ее объявления.
	
	В отличие от Function Declaration, Function Expression не загружаются в контекст выполнения, а запускаются только тогда, когда программа сталкивается с ними.

Function Expressions vs Function Declarations
	Крч никто нормально не говорит когда что использовать, поэтому можно сделать вывод: что хош то и используешь
	Но пару рекомендаций нашел:
		1. Если функции нужны только один раз и вам достаточно, чтобы она вызвалась один раз и мы могли забыть о ней, то выражение функции - хороший выбор.
		
Arrow Function
	const sum = (a, b) => a + b
	Стрелочные функции всегда анонимны - нет возможности дать имя стрелочной функции.
	
IIFE (Немедленно вызываемые выражения функций)
	Когда функция создается одновременно с ее вызовом, вы можете использовать IIFE, которое выглядит следующим образом:
		(function() => {})();
	или:
		(() => {})();
		
	Чаще всего область видимости функции в IIFE используется для предотвращения утечки локальных переменных в глобальную область видимости.
	Защитная точка с запятой в конце IIFE существует для предотвращения проблем, которые могут возникнуть при конкатенации двух файлов JavaScript.
	
	Именованные IIFE
		Ниче не поменяется, но имя у функции будет :)
		(function iife() {
			// ...
		})();
		
	
	Крч, IIFE стоит использовать только из-за того, что ты не можешь использовать var, т.к. все примеры использования относятся к var
	Use cases:
		1. Локальные переменные, объявленные с помощью ключевого слова var, привязываются к объемлющей функции. Если такой функции не существует, переменные будут созданы как глобальные переменные, тем самым загрязняя глобальную область видимости. Чтобы предотвратить это, мы можем использовать IIFE для создания функции-обертки
			(function () {
				var foo = "bar";
				console.log(foo);
			})();

			foo; // ReferenceError: foo is not defined
		2. So I used an IIFE to create my array (хз, говорят что это антипаттерн):
				const faces = (() => {
						const faces='😀😃😄😁😆😅😂🤣😊😇🙂🙃😉😌🎃';
						const faceArray = [];
						for(let index = 0; index < faces.length; index += 2) {
								let face = faces[index] + faces[index+1]
								faceArray.push(face)
						}
						return faceArray
				})();
			
	С учетом сказанного, IIFE уже практически устарел, поскольку теперь мы можем объявлять области видимости на уровне блоков. Однако IIFE все еще распространены из-за обратной совместимости.
		// Это работает только для переменных блочного диапазона (т.е. `const` и `let`).
		{
			const inside = 'bar';

			console.log(outside); 'foo'
			console.log(inside); 'bar'
		}
				
Callbacks
	Функция, переданная другой функции
		function mapAction(item) {
			// do stuff to an item
		}
		array.map(mapAction)
	Проблема здесь в том, что mapAction будет доступен всему вашему приложению - в этом нет необходимости.
		array.map(item => { //do stuff to an item })
	или:
		const mapAction = function(item) {
			// do stuff to an item
		}
		array.map(mapAction)	
	
	Пример callback:
		function getNewDate(callback) {
			setTimeout(function () {
				callback(new Date());
			}, 4000);
		}

		function getPayload(payload) {
			console.log(`The date is: ${payload}`);
		}

		getNewDate(getPayload);

NFE (Named Function Expression)
	let sayHi = function func(who) {
		alert(`Hello, ${who}`);
		func("Guest"); // Можем
		sayHi('Guest) // Можем (Но переменная sayHi может поменяться на другую при передаче функции другой переменной)
	};
	
	Есть две важные особенности имени func, ради которого оно даётся:
		Оно позволяет функции ссылаться на себя же.
		Оно не доступно за пределами функции.
	
	Почему мы используем func? Почему просто не использовать sayHi для вложенного вызова? 
	Можем, Однако, у этого кода есть проблема, которая заключается в том, что значение sayHi может быть изменено.
	
new Function
	let sum = new Function('a', 'b', 'return a + b');
	
	Когда вы создаете функцию, применяя new Function, [[Environment]] ссылается не на текущее Лексическое окружение, а на глобальное.
	Используется когда функция получается откуда-то (например от сервера)
	
Anonymous Functions
	Функция без имени называется анонимной функцией.
		var b = function (){
			console.log("This is a function expression example")
		}
	ИЛИ
		() => {}

Constructors
	Функции-конструкторы технически являются обычными функциями. Но есть два соглашения:
		1. Имя функции-конструктора должно начинаться с большой буквы.
		2. Функция-конструктор должна выполняться только с помощью оператора "new".
		
		function User(name) {
			// this = {};  (неявно)
			this.name = name;
			this.isAdmin = false;
			// return this;  (неявно)
		}
		let user = new User("Jack");
	
	Когда функция вызывается как new User(...), происходит следующее:
		1. Создаётся новый пустой объект, и он присваивается this.
		2. Выполняется тело функции. Обычно оно модифицирует this, добавляя туда новые свойства.
		3. Возвращается значение this.
		
Constructor vs Object Literal
	Объектный литерал обычно используется для создания одного объекта, в то время как конструктор полезен для создания нескольких объектов
		// Объектный литерал
		let user = {
				name: 'Bob'
		}
		// Конструктор
		function User() {
				this.name = 'Bob';
		}

Accessor Function Initializer
	var obj = {
		  value: 0,
		  get f() {
		      return this.value;
		  },
		  set f(v) {
		      this.value = v;
		  }
	};
